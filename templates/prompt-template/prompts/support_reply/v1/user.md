<!--
  user template — v1 (immutable once shipped)
  TODO: adjust the fields you pass in. Every {{placeholder}} here (and in
  system.md) must be supplied to registry.load(..., variables={...}).
-->
A customer named {{customer_name}} wrote in:

"{{customer_message}}"

Write a reply addressed to {{customer_name}}.
