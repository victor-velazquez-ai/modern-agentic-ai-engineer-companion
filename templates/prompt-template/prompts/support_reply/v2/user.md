<!--
  user template — v2 (immutable once shipped)
  Same variables as v1 so the two versions are drop-in swappable for A/B and
  rollback. TODO: adjust the fields you pass in to match your data.
-->
A customer named {{customer_name}} wrote in:

"{{customer_message}}"

Write a reply addressed to {{customer_name}}, following the three-beat structure.
