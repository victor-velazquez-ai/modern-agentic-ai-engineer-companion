# ─────────────────────────────────────────────────────────────────────────────
# Secrets — NEVER commit. Only `.env.local.example` (no real values) is tracked.
# ─────────────────────────────────────────────────────────────────────────────
.env
.env.local
.env.*.local

# Dependencies
/node_modules
/.pnp
.pnp.*

# Next.js build output
/.next/
/out/
/build
next-env.d.ts

# Production
/dist

# Misc / OS
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
