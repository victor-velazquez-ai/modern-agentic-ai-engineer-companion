"""Your agent application package.

The src/ layout means this package is importable as `app` once installed
(`uv sync`) or with `src` on the path (pytest is configured for that).
"""

from __future__ import annotations

__version__ = "0.1.0"
