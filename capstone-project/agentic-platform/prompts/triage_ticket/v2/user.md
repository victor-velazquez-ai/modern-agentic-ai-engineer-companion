<!--
  user template — v2 (immutable once shipped)
  Every {{placeholder}} here (and in system.md) must be supplied to
  registry.load(..., variables={...}).
-->
Triage this support ticket:

"{{ticket_text}}"

Return JSON with: category (one of the allowed categories), priority (p0–p3),
confidence (0–1), and rationale (one sentence).
