"""Scoped action tools for the customer-support solution.

The agent's *act* surface lives here, behind the ``mcp-server`` pattern blueprint's safe
consumption boundary. See :mod:`tools.billing_mock`.
"""
