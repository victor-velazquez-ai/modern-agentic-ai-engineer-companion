# Importing data

Nimbus imports data from CSV, JSON, and direct database connections. Open a project, click
**Import**, choose your source, and map the columns. Large imports run in the background and you
receive an email when they finish. Imports are incremental: re-running an import only adds new or
changed rows, it never duplicates existing ones.
